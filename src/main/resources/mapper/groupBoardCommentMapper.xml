<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="GroupBoardComment">

    <!-- 댓글 저장 -->
    <insert id="save" parameterType="groupBoardComment">
        INSERT INTO groupboardcomment (groupId, boardId, author, content)
        VALUES (#{groupId}, #{boardId}, #{author}, #{content})
    </insert>

    <!-- 댓글 목록 -->
    <select id="findByBoardId" parameterType="int" resultType="groupBoardComment">
        SELECT * FROM groupboardcomment
        WHERE boardId = #{boardId}
        AND status = 1
        ORDER BY createdAt ASC
    </select>

    <!-- 댓글 삭제 -->
    <update id="delete" parameterType="int">
        UPDATE groupboardcomment
        SET status = 0
        WHERE id = #{id}
    </update>

</mapper>