<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Group">
    <insert id="createGroup" parameterType="com.spring.group.dto.GroupDTO">
        INSERT INTO groups (leader, title, description, location, max_user_num, created_at, status)
        VALUES (#{leader}, #{title}, #{description}, #{location}, #{maxUserNum}, NOW(), 1)
    </insert>





</mapper>